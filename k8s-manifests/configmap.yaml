apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: todo-app
data:
  # MongoDB connection - using host network since MongoDB runs on VM
  MONGO_HOST: "192.168.74.128"
  MONGO_PORT: "27017"
  # Application settings
  NODE_ENV: "production"
  PORT: "4000"
